# frozen_string_literal: true

RSpec.describe CoreNutrition::Partner::Client do
  it "has a version number" do
    expect(CoreNutrition::Partner::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
